import Link from 'next/link'
import React from 'react'

const SignInButton = () => {
  return (
    <Link href={'/'} onClick={() => {}} className='text-white/60 font-thin underline'> 
      Sign - In
    </Link>
  )
}

export default SignInButton